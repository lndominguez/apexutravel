// Sistema de loading unificado para Travel CRM
// GlobalLoading: Para refresh del navegador (pantalla completa)
// ContentSkeleton: Para contenido de páginas (dentro del layout)

export { GlobalLoading } from '../GlobalLoading'

export { 
  ContentSkeleton,
  DashboardSkeleton,
  TableSkeleton,
  CardsSkeleton,
  FormSkeleton,
  ProfileSkeleton
} from '../ContentSkeleton'
